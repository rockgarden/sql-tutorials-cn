# [理解NewSQL数据库](https://www.baeldung.com/sql/database-newsql-intro)

数据库概念

1. 概述

    数据库是我们存储数据集合的一种方式，目前有多种类型的数据库可供选择。不同的数据库管理系统（如 SQL 和 NoSQL 数据库）因其性能、一致性等需求以及待存储的数据类型（结构化、无模式等）而流行。现在，我们有了所谓的 **NewSQL 数据库**，它们结合了 SQL 和 NoSQL 数据库的优势。

    在本教程中，我们将先回顾 SQL 和 NoSQL 数据库，然后深入理解 NewSQL 数据库的特点。

2. SQL 和 NoSQL 数据库

    几十年来，传统的 SQL 数据库一直是数据存储和检索的基础。SQL 数据库提供了强大的 **ACID 合规性**，确保了可靠性、一致性和数据完整性。其常见用例包括 OLTP 和 OLAP 应用程序以及数据仓库应用。

    然而，随着数字化格局的变化，数据库的需求也在发生变化。互联网的兴起带来了海量数据，多个来源瞬间生成大量数据。尽管传统 SQL 数据库在一致性和可靠性方面表现出色，但它们难以应对这些快速流动数据流的需求。

    因此，NoSQL 数据库成为了一种可行的替代方案。NoSQL 数据库将性能、可扩展性和灵活性置于 ACID 合规性之上。它们使用多种数据模型（[文档](https://www.baeldung.com/java-orientdb)、键值、列族等），使其在某些特定用例中表现出色，例如分布式系统、实时分析和非结构化数据存储。社交媒体应用和文档导向型应用是其最常见的用例之一。

    虽然 NoSQL 数据库解决了可扩展性问题，但也带来了一些权衡，最显著的是放松了一致性模型。在需要强数据一致性和事务保证的情况下，NoSQL 数据库显得力不从心。这促使人们需要一种新型数据库系统，能够兼具 SQL 和 NoSQL 的优势。

3. NewSQL 数据库

    NewSQL 数据库试图解决现有数据库的局限性。它们被设计为具有分布式和容错架构的关系型数据库。NewSQL 数据库旨在通过以下特性提供解决方案：

    - **可扩展性和 ACID 合规性**：NewSQL 数据库设计为水平扩展，通过将数据分布到多个节点/集群来处理大量数据。同时，它们保持严格的 ACID 合规性，从而构建一个高可用性且具有强大事务完整性的系统。
    - **性能优化**：采用多种技术（如内存处理、索引和缓存）以实现低延迟的数据访问和高性能。
    - **分布式架构**：使用多个节点复制数据，避免单点故障，从而确保高可用性和容错能力。
    - **SQL 兼容性**：NewSQL 系统兼容 SQL 查询语言，避免了重新学习和迁移的开销。

    1. 使用场景

        NewSQL 数据库最适合那些需要强事务一致性，同时兼顾高性能和可扩展性的应用。以下是一些常见的使用场景：

        - 金融系统：需要以低延迟和高精度处理大量数据。
        - 电子商务平台：尽管负载相对稳定且可预测，但偶尔可能会出现需要支持的流量激增。
        - 实时反馈系统：依赖实时数据分析的系统（如航空定价、欺诈检测等）可以从高性能事务系统中受益。
        - 智能设备与智慧城市：随着多个领域和用例中自动化程度的提高，持续的数据流可以利用 NewSQL 数据库高效处理。

        然而，在某些情况下，NewSQL 数据库可能并不适合：

        - 成熟的数据模型：对于已有成熟数据模型且负载或扩展能力可控的应用（如遗留系统），迁移到 NewSQL 数据库可能不合适。
        - 可预测的负载：对于负载可预测且不需要动态扩展的应用。
        - 严格的 ACID 合规性：SQL 数据库更适合对 ACID 属性要求极为严格的应用。

    2. 流行的 NewSQL 数据库

        以下是一些流行的 NewSQL 数据库：

        - [CockroachDB](https://www.baeldung.com/cockroachdb-java)：一个开源的分布式数据库，设计用于在各种故障中生存，同时保持 ACID 合规性。它采用分布式架构，提供强大的故障转移能力和自动数据复制。
        - [NuoDB](https://doc.nuodb.com/nuodb/latest/introduction-to-nuodb/)：它使用专利的“弹性扩展数据库”架构，在保留 ACID 合规性的同时提供 NoSQL 的优势。
        - [VoltDB](https://github.com/VoltDB)：一个基于共享无架构设计的内存数据库，适用于高速数据摄入。

    3. 缺点

        尽管 NewSQL 数据库解决了 SQL 和 NoSQL 数据库的局限性，但它们也存在一些自身的缺点：

        - 特定于用例：不同的 NewSQL 数据库适合不同的用例，并没有一种适用于所有场景的解决方案。
        - 复杂的学习曲线：由于 NewSQL 数据库倾向于针对特定用例，每次新实施可能都需要面对一条非重复的学习曲线。
        - 兼容性问题：NewSQL 数据库可能不总是与现有的数据模型和模式兼容，这可能导致大量的迁移工作。

4. 结论

    在本文中，我们探讨了从传统 SQL 到 NoSQL 数据库，再到 NewSQL 数据库的数据存储和检索的演变过程。我们还研究了 NewSQL 数据库的不同使用场景以及一些流行的 NewSQL 数据库。

    NewSQL 数据库通过结合事务一致性、可扩展性和高性能，弥合了 SQL 和 NoSQL 数据库之间的差距。我们看到了一些使用 NewSQL 数据库可能有益的场景。
